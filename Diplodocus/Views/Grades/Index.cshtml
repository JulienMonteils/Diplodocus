@model IEnumerable<Diplodocus.Models.Grade>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.gradeName)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.gradeName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.IdGrade }) |
                @Html.ActionLink("Details", "Details", new { id=item.IdGrade }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IdGrade })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
