@model IEnumerable<Diplodocus.Models.StudentMarkSubject>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.aSchoolSubjectMark.IdMark)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student_idUser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SchoolSubject_idSubject)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.aSchoolSubjectMark.IdMark)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student_idUser)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SchoolSubject_idSubject)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.IdStudentMarkSubject }) |
                @Html.ActionLink("Details", "Details", new { id=item.IdStudentMarkSubject }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IdStudentMarkSubject })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
